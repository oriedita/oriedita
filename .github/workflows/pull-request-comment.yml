name: Create Pull Request Comment

on:
  workflow_run:
    workflows: [Create artifact]
    types: [completed]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: View context attributes
        uses: actions/github-script@v5
        with:
          script: console.log(github.event.workflow_run)
      - uses: marocchino/sticky-pull-request-comment@v2
        with:
          number: github.event.workflow_run.pull_requests[0].number
          message: |
            Hello #${{ github.event.workflow_run.pull_requests[0].number }} [Latest jar for this branch](https://nightly.link/qurben/origami-editor/actions/runs/${{ github.event.workflow_run.run_id }}/origami-editor-jar.zip)